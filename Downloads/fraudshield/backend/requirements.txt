# PINNED VERSIONS — see app/services/model_service.py for why scikit-learn is pinned exactly.
# The bundled preprocessor.pkl / random_forest.pkl were trained under scikit-learn 1.6.1.
# preprocessor.pkl FAILS TO LOAD under scikit-learn >= 1.8 (ColumnTransformer internals changed).
# Do not bump scikit-learn without re-running tests/test_model_integration.py against the new version.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
scikit-learn==1.6.1
pandas==2.2.3
numpy==1.26.4
joblib==1.4.2
shap==0.46.0
python-multipart==0.0.20
pytest==8.3.4
httpx==0.28.1
