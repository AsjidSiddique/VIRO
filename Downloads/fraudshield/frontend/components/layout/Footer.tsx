export function Footer() {
  return (
    <footer className="border-t border-border py-8">
      <div className="mx-auto flex max-w-7xl flex-col items-center justify-between gap-3 px-4 text-sm text-muted sm:flex-row sm:px-6">
        <p>FraudShield — a research/portfolio project built on the public Kaggle Credit Card Fraud dataset.</p>
        <p>Not a real banking production system.</p>
      </div>
    </footer>
  );
}
