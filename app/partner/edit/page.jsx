import PartnerEditClient from './PartnerEditClient'

export const metadata = {
  title: 'Edit My Info — Viro Partner',
  robots: { index: false, follow: false },
}

export default function PartnerEditPage() {
  return <PartnerEditClient />
}
